{
  "name": "saas-template-may-2025",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "electron-app",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "dev:electron": "bun --filter @saas-template-may-2025/electron-app dev",
    "dev:electron-server": "NO_ELIDE_LOGS=1 bun --filter @saas-template-may-2025/electron-app --filter server dev",
    "rebuild:electron:arm": "bun --filter @saas-template-may-2025/electron-app native-modules:rebuild:arm",
    "build:shared": "cd shared && bun run build",
    "build:server": "cd server && bun run build",
    "build:client": "cd client && bun run build",
    "build:electron": "cd electron-app && bun run build",
    "build": "bun run build:shared && bun run build:server && bun run build:client && bun run build:electron",
    "start": "bun --filter '*' start",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "@types/bun": "^1.2.5",
    "@types/cookie": "^0.6.0",
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "concurrently": "^9.1.2",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "@types/papaparse": "^5.3.15",
    "cheerio": "^1.0.0",
    "lucide-react": "^0.483.0",
    "papaparse": "^5.5.2"
  },
  "resolutions": {}
}
